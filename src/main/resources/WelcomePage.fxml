<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.*?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml"
           fx:controller="org.example.workspace.controllers.WelcomeController"
           styleClass="welcome-root">

    <VBox alignment="CENTER" spacing="40" maxWidth="550">
        <padding>
            <Insets top="80" right="60" bottom="80" left="60"/>
        </padding>

        <VBox alignment="CENTER" spacing="25">
            <Label text="⚡" style="-fx-font-size: 72px;"/>
            <Label text="TaskMaster" styleClass="welcome-title"/>
            <Label text="Professional Task Management" styleClass="welcome-subtitle"/>
            <Label text="Organize, Track, and Achieve Your Goals" styleClass="welcome-description"/>
        </VBox>

        <HBox spacing="15" alignment="CENTER">
            <VBox alignment="CENTER" spacing="8" styleClass="feature-pill">
                <Label text="📊" style="-fx-font-size: 28px;"/>
                <Label text="Analytics" styleClass="feature-label"/>
            </VBox>
            <VBox alignment="CENTER" spacing="8" styleClass="feature-pill">
                <Label text="📅" style="-fx-font-size: 28px;"/>
                <Label text="Calendar" styleClass="feature-label"/>
            </VBox>
            <VBox alignment="CENTER" spacing="8" styleClass="feature-pill">
                <Label text="🎯" style="-fx-font-size: 28px;"/>
                <Label text="Goals" styleClass="feature-label"/>
            </VBox>
        </HBox>

        <VBox spacing="18" alignment="CENTER" VBox.vgrow="ALWAYS">
            <Button fx:id="loginButton"
                    text="Sign In to Your Account"
                    onAction="#onLogin"
                    styleClass="welcome-btn-primary"
                    prefWidth="360"
                    prefHeight="58"/>

            <Button fx:id="registerButton"
                    text="Create New Account"
                    onAction="#onRegister"
                    styleClass="welcome-btn-secondary"
                    prefWidth="360"
                    prefHeight="58"/>
        </VBox>

        <VBox alignment="CENTER" spacing="8">
            <Label text="© 2025 TaskMaster" styleClass="welcome-footer"/>
            <Label text="OOP Final Project" styleClass="welcome-footer-sub"/>
        </VBox>
    </VBox>

</StackPane>
